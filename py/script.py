def multiply(a,b):
    result = a * b

    # Begin of injected codes:
    # from datetime import datetime as dt
    # with open("/work/hogehoge.txt", "a") as f:
    #   f.write("%s %d\n"%(dt.now().isoformat(' '), result));
    # End of injected codes:

    return result
